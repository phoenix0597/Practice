scores = [8, 5, 10, 7, 6]
scores[1] += len(scores)
scores.append(0)
scores[2] += len(scores)
print(scores)

# c = [10, 20, 30, 40]
# c[0] - это первый элемент списка
# c[-1] - это последний элемент списка (первый элемент списка справа налево
# c[-2] - это предпоследний элемент списка, соответственно (второй справа налево)
